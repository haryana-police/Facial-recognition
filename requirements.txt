# ============================================================
# Forensic Facial Recognition System - Requirements
# ============================================================
# 1. Install PyTorch FIRST (with CUDA if available):
#    GPU:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#    CPU:  pip install torch torchvision
# 2. Then: pip install -r requirements.txt
# ============================================================

# Core API
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
aiofiles==23.2.1
pydantic==2.7.4

# Image Processing
opencv-python-headless==4.9.0.80
Pillow==10.3.0
numpy==1.26.4

# Stage 1 - Enhancement (CodeFormer / GFPGAN)
basicsr==1.4.2
facexlib==0.3.0
gfpgan==1.3.8

# Stage 2 - Face Detection (YOLOv8 + RetinaFace fallback)
ultralytics==8.2.38
retina-face==0.0.17

# Stage 3 - ArcFace Embeddings (InsightFace buffalo_l)
insightface==0.7.3
onnxruntime==1.18.1            # swap for onnxruntime-gpu on GPU machines

# Stage 4 - Similarity
scipy==1.13.1

# Utilities
python-dotenv==1.0.1
